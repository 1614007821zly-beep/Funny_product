# 本地 AI 与完整流程验收

日期：2026-09-01。基于现有 V50 工作区，对 V51 候选版本执行上线前本地验收。

## 结论

本地持久闭环已补齐并通过验证：灵感路线随安排保存；天气只展示当前城市的最新响应；个人安排由本人确认完成；共同安排必须由两位不同参与者确认；完成与基础回忆在同一数据库事务中持久化；各自的文字与照片由各自控制。

本报告记录发布前的本地验证结果。`0008` 迁移随 V51 发布包上线，在部署完成后还需执行线上双账号回归。

## 本轮修正

1. **天气竞态**：每次请求带有本地代次，旧城市或较慢的旧响应无法覆盖新城市；页面显示前再次核对查询城市。AI 生成返回的天气也会使先前独立天气请求失效。
2. **安排事实快照**：采用 AI 灵感时，保存有界的标题、城市、简介、时长、预算说明、时间线和所选地点。重新打开安排时只读取该安排自己的快照，不再依赖当前灵感或演示内容。
3. **来源说明**：保存的地点被标为“采用安排时保存的快照”，不冒充实时高德结果；营业、价格和距离仍提示出发前核实。旧安排没有快照时明确显示缺失，不自动补造路线。
4. **完成状态机**：个人计划仅本人确认；共同安排先由一人请求完成，再由另一位参与者独立确认。未来、未接受、取消、删除的安排不能完成。重复提交返回同一结果；安排编辑会使待完成确认失效并要求重新接受。
5. **原子回忆生成**：完成状态和每位参与者的基础回忆在同一事务写入。基础回忆仅包含保存过的事实，不编造见面地点、行程、感受或消费。
6. **回忆归属**：手动回忆和个人补充持久保存到 D1。共同安排的双方各有自己的事实副本；文字与照片默认不共享，主动勾选后才对 TA 开放。分享内容必须先撤回才能删除自己的副本。
7. **照片权限**：照片上传后先保持个人可见；主动分享回忆时才切换为共同可见。撤回会同时更新 D1 权限并删除 R2 文件；安全退出会撤回本人已分享的文字与照片。
8. **列表与导出**：已完成安排不再占用首页“下一件小事”；仍保留在日历并显示“已完成”。账户导出包含安排快照、完成信息和本人回忆。
9. **新建体验**：新建回忆不再预填虚构的书店、城市和文案，只保留当天日期及账户城市，其他内容由用户主动填写。

## 验证结果

- `npm test`：类型检查、生产构建和 **52 项离线测试全部通过**。
- `npm run lint`：通过。
- `git diff --check`：通过。
- `node tests/local-flow-smoke.mjs`：本地双账号真实 D1/R2 流程通过，包括绑定、个人隔离、分享、接受、公开链接撤回、双方完成、重复请求幂等、双方回忆生成、文字与照片分享/撤回、安全退出及重新绑定。
- 浏览器回归：新建回忆无虚构默认值；保存、刷新、新窗口读取、编辑、删除均通过。过去的个人安排可查看保存地点与预算，可展开保存路线，确认完成后自动出现事实回忆，且不再显示编辑入口或虚构地点。
- 测试产生的可识别浏览器安排与回忆已精确清理；隔离流程使用随机账号并在结束时退出关系。

## 本地数据库

- 生成并检查了 `drizzle/0008_uneven_kree.sql`，新增回忆表、完成请求人和完成时间。
- 执行前备份：`/private/tmp/love-diary-before-0008.aWJWb6/before-0008.sqlite`。
- 只对 `.wrangler/state/v3/d1/` 下本地库执行 `0008`；迁移后 `PRAGMA integrity_check` 返回 `ok`。
- 没有操作线上数据库；本地仍可能保留随机测试账号及只读归档记录，不影响线上用户。

## 尚未声称完成的事项

- 本地隔离身份不等于真实 ChatGPT 登录；线上迁移、真实双账号和真实设备定位仍需发布前单独执行。
- 高德价格、营业状态和距离不是实时成交或到店证明；保存快照只是采用安排时的参考。
- 免费 AI 的质量和时延仍会波动；格式与硬条件校验通过不代表推荐必然足够新奇。
- 离线导出、截屏或对方已另行保存的文件无法由平台远程删除，界面继续明确说明此边界。

## 本地入口

本地服务：`http://localhost:3000/`。隔离测试入口：`http://127.0.0.1:3200/`（账号 A）与 `http://127.0.0.1:3201/`（账号 B）。隔离代理只绑定本机且不会随产品部署。
